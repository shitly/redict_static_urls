import pandas as pd 
import numpy as np
import re

import os
from os.path import join, dirname, abspath
DIR = dirname(dirname(abspath(__file__)))

import sys
sys.path.insert(0, DIR)
# 处理规则文件夹tags = [True if x is '..' else False  for x in ss ]  ss = "../../demo/ko/op.html".split("/")



#### 重定向文件设置 ######## 